import React from "react";
import DescripSection from "../Components/DescripSection";
import Footer from "../Components/Footer";

const Buses = () => {
  // this is the Bus page as you can see it is made up of three main component the Header, DescripSection and the Footer
  return (
    <div>
      {/* Hold contraol + click on any of the three components to know more (code) */}
      <DescripSection />
    </div>
  );
};

export default Buses;
